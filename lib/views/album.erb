<h2>Albums of <%= "#{@results[0][0]}"%></h2>
<p>
  <ul>
    <% @results.each do |track| %>
      <li>
        <a href=<%="/tracks/#{track[2]}"%>>
          <%= track[1] %>
        </a>
      </li>
      <% end %>
  </ul>
</p>
